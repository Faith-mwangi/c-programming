/*arithmetic operations*/
#include <stdio.h>
int main()
{
    int a,b,add,subtract,divide,multipy,reminder;
    printf("enter a,b values");
    scanf("%d%d",&a,&d);
    add=a+b;
    subtract=a-b;
    divide=a/b;
    multiply=a*b
    reminder=a%b;
    printf("results of add is=%d/n",add);
    printf("results of subtract is=%d/n",subtract);
    printf("results of divide is=%d/n",divide);
    printf("results of multiply is=%d/n",multiply);
    printf("results of reminder is=%d/n",reminder);
    return 0;
}
